# Production API URL (for deployment)
NEXT_PUBLIC_API_URL=https://mcdonalds-malaysia-api.onrender.com

# Development API URL (for local development with HTTPS)
# Note: When running with npm run dev:https, the app uses Next.js proxy
# to avoid CORS issues. The proxy forwards requests to this URL.
# NEXT_PUBLIC_API_URL=http://localhost:8000

# For regular HTTP development (npm run dev)
# NEXT_PUBLIC_API_URL=http://localhost:8000

# Environment mode
NODE_ENV=development
